package day2_webdriver_basics;

public class WebDriverFactory {

    // wrtie a static method that takes a string parameter
    //based on value of the parameter
    //the method must return chromdriver or firefoxdriver object
    //name : getDriver

   /*public static WebDriver getDriver (String browersType){
    WebDriver driver =null;
    swith(browserType)

    case : "chrome" :
        WebDriverManager.chromdriver(){
        driver = new ChromeDriver();
        break;

       case "firefox" :
       webDriverManager.firefoxdriver().setup();
       driver = new FirefoxDriver();

   }

   return driver;

   */




}
